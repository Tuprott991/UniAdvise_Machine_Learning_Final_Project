/**
 * Re-exports all exports from the `universitiesApi` module.
 *
 * @remarks
 * This allows other modules to import all functionalities related to university API
 * endpoints from a central location, simplifying imports and improving modularity.
 *
 * @example
 * ```ts
 * import { getAllUniversities, getUniversityById } from './api';
 * ```
 */
export * from './universitiesApi';
